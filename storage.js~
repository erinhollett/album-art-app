// === Save to storage === //
function saveImage(albumName, date, imageCanvas) {
}

// === Retrieve list of saved images === //
function getImageList() {
}

// === Retrieve and deserialize image === //
function getImage(imageId) {
}

